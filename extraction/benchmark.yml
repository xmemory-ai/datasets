name: "xmemory extraction benchmark for insurance claims from cleanlab (fixed labels)."
author: AlexG
created_at: 2026-02-09

api_config:
  xmemory:
    extraction_logic: deep
    max_parallel_requests: 10

dataset:
  local_config_path: datasets/dataset.yml

num_of_runs: 10

metrics:
  extract:
    - extraction_exact_match
